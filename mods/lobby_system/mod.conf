name = lobby_system
title = Lobby System
description = Reusable multiplayer lobby framework: formspec panel, join/leave/start flow with a countdown and late-join window, persistent scorekeeping, event announcements, and lobby/win/lose sounds. Games built on top of it register themselves and get placed onto whatever the host game provides (or open space if not).
depends =
